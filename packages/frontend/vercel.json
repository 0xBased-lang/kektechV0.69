{
  "buildCommand": "cd ../.. && npm install && cd packages/frontend && npx prisma generate && npm run build",
  "installCommand": "cd ../.. && npm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
